[project]
name = "tool-update-auditor"
version = "0.1.0"
description = "Windows workstation inventory and update-policy auditor."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "amibars" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
